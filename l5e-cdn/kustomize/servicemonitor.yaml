apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: l5e-cdn
  namespace: l5e
  labels:
    release: kube-prometheus-stack   # ← FIXED
spec:
  selector:
    matchLabels:
      app: l5e-cdn
  namespaceSelector:
    matchNames:
      - l5e
  endpoints:
    - port: http
      interval: 15s
      path: /metrics
