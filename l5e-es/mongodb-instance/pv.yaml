apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-l5e-es
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-l5e-es
  local:
    path: /run/media/at-1/es
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - toph
