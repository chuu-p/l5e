apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: mongodb-instance
  namespace: l5e
resources:
  - namespace.yaml        # if you need to ensure namespace l5e exists
  - storageclass.yaml
  - pv.yaml
  - service.yaml
  - ingress.yaml
  - mongodb-cr.yaml       # MongoDBCommunity custom resource
